<%= turbo_frame_tag "room_#{room.id}", class: 'room-container' do %>
  <%= link_to room_path(room), class:"room-name", data: { turbo_frame: 'rooms_controller' } do %>
    <%= room.name %>
  <% end %>
  
  <%#= link_to edit_room_path(room), id: "edit_room_#{room.id}", class: 'edit-link' do %>
   <!-- Edit -->
  <%# end %>
<% end %>
